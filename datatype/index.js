/*DataTypes:

    1.primite datatype(Stake memory used)(stack store copy of the value)
        number=>2 to power 53,
        Bitint,
        string =>" ",
        boolean =>true/false,
        null =>standalone value,
        undefined => value not assign
        symbol => unique 
    2.non-primitive datatype(Heap memory used)(Heap stored original value)
        Object => 
        Array =>
        Functions =>

            console.log(typeOf(null));//object
            console.log(typeOf(undefined));//undefined
*/
    